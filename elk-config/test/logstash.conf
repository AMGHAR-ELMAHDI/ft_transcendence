input {
  beats {
    port => 5044
  }
}

filter {
  # Add your desired filters here
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "your_index_name-%{+YYYY.MM.dd}"
  }
}